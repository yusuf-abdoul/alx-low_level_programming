#include <stdio.h>

/**
 * main - Entry point
 *
 * This is the main functio, where everything holds
 * Return: Always 0(Success)
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle\n");

	return (0);
}
