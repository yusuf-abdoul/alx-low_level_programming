#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

void positive_or_negative(int i);
int largest_number(int x, int y, int z);
int convert_day(int monthes and int days);
void print_remaining_days(int month, int day, int year);

#endif /*MAIN_H*/
